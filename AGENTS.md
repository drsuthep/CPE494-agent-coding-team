# AGENTS.md

This file gives Codex and other AI coding assistants the operating rules for the CPE494 Agent Coding Lab.

## Project Summary

This project is a teaching lab for **CPE494**. The goal is to demonstrate how AI agents can work together as a software engineering team to build an enterprise-style application from scratch.

The demo application is an **ERP Invoice application**. Students will study this demo, then adapt the same approach to build a different application such as a Student Registration app.

The project intentionally starts with **pure Python orchestration** instead of ADK or MCP so students can clearly understand the agent workflow. Later, students may convert the workflow to use **Google ADK** and **MCP tools**.

## Repositories

This lab uses two sibling repositories under one parent folder.

```text
lab-coding-agent/
  CPE494-agent-coding-team/
  CPE494-erp-invoice-app-by-ai/
  lab-coding-agent.code-workspace
```

### CPE494-agent-coding-team

This repository contains the AI agent coding team.

It includes:

- Python orchestration notebook
- Gemini API client logic
- agent role prompts
- logic specifications
- UI specifications
- runtime logs
- generated artifacts
- audit reports

This repo is the AI software team.

### CPE494-erp-invoice-app-by-ai

This repository contains the ASP.NET Core ERP Invoice application generated by the AI agents.

This repo should stay clean as the target application codebase. It should not contain agent logs or orchestration artifacts. Git history records which generated code was accepted and committed.

## Source Specification Files

The following files are the main source of truth for this project:

```text
logic_specs/erp_invoice_logic.md
ui_specs/erp_invoice_ui.md
ui_specs/ui_reference.pdf
```

Use them as follows:

- `logic_specs/erp_invoice_logic.md`: non-UI business, architecture, data, security, validation, and coding rules.
- `ui_specs/erp_invoice_ui.md`: UI/UX rules, Zen Green theme, layout, list views, form views, validation UX, LoV, and responsive behavior.
- `ui_specs/ui_reference.pdf`: visual reference showing the intended app shell, login screen, list views, form views, validation behavior, LoV behavior, and responsive layout.

Important: The PDF contains Thai labels in some screenshots. These are visual references only. The generated application must use **English only**.

## Required Agent Repo Structure

```text
CPE494-agent-coding-team/
  notebooks/
    agent_coding_workflow.ipynb

  prompts/
    role_architect.txt
    role_coder.txt
    role_logic_tester.txt
    role_ui_auditor.txt

  logic_specs/
    erp_invoice_logic.md

  ui_specs/
    erp_invoice_ui.md
    ui_reference.pdf

  outputs/
    runs/
      <run_id>/
        run_manifest.json
        input_snapshot/
        input_snapshot_manifest.json
        gemini_calls.jsonl
        architect_manifest.md
        tasks.json
        attempts/
        generated_files/
        audit_logs.jsonl
        workflow_result.json
        applied_files.json
        command_logs.jsonl
    latest_run.txt

  .env
  .gitignore
```

## Prompt and Specification Separation

Keep prompts and specifications separate.

```text
prompts/      = how agents behave
logic_specs/  = what the app must do
ui_specs/     = how the app must look
```

Do not move project requirements into role prompts unless they are truly about agent behavior.

## Main Agent Roles

### Architect Agent

The Architect analyzes requirements and produces:

- human-readable technical manifest
- file plan
- task list
- JSON task list for the orchestrator

The Architect does **not** write code.

Architect must enforce:

- English-only UI
- database primary key named exactly `id`
- every table/entity includes `rowversion`
- header + line item saves use atomic transactions
- logic/database naming should use `snake_case`
- UI must follow Zen Green theme
- output must end with a machine-readable JSON task list

### Coder Agent

The Coder generates raw source code.

The Coder must output only source code. Do not include Markdown fences or explanations.

The Coder must follow:

- English-only UI
- `snake_case` naming for C# logic and database properties where required
- `id` primary key
- `rowversion`
- transactional save logic where needed
- Zen Green theme
- right-aligned numeric and currency fields
- English tooltips on buttons
- red asterisks for required fields
- appropriate validation display

For UI-related files such as `.cshtml`, `.css`, layout files, and form/list pages, the Coder should receive a compact UI checklist from `ui_specs/erp_invoice_ui.md`.

### Logic Tester Agent

The Logic Tester audits generated code for:

- database integrity
- `id` primary key
- `rowversion`
- `snake_case` naming
- transaction safety
- syntax issues
- dependency injection
- parameterized queries or EF Core usage
- input validation
- English-only code/messages

The Logic Tester must respond with `PASS` if the file passes. Otherwise, it must return a concise list of issues.

### UI Auditor Agent

The UI Auditor checks:

- English-only UI
- Zen Green theme
- dark green headers/title strips, preferably `#004d40`, with white text
- light green/mint accents
- rounded borders
- Tabulator grid styling where grids are used
- right-aligned numeric/currency fields
- English tooltips for all action buttons
- Font Awesome icons where appropriate
- red asterisks for required fields
- header-field validation as red text below fields
- line-item/grid validation as floating tooltip-style messages
- responsive attribute-value header layouts
- horizontal scrolling for grids on small screens

The UI Auditor must respond with `PASS` if the file passes. Otherwise, it must return a concise list of discrepancies.

## Target App Technology Stack

The generated app is:

- ASP.NET Core Razor Pages
- C#
- Entity Framework Core
- CSS
- Tabulator for grids
- Font Awesome for icons
- Zen Green theme

## Runtime Logging and Traceability

Every notebook run must create a unique run folder.

```text
outputs/runs/<run_id>/
```

Each run folder should contain:

```text
run_manifest.json
input_snapshot/
input_snapshot_manifest.json
gemini_calls.jsonl
architect_manifest.md
tasks.json
attempts/
generated_files/
audit_logs.jsonl
workflow_result.json
applied_files.json
command_logs.jsonl
```

### run_manifest.json

Stores:

- run ID
- sprint ID
- sprint goal
- timestamp
- agent repo Git commit hash
- target app repo Git commit hash
- Git status information
- input file hashes

### input_snapshot/

Copies of all prompts and specs used in the run.

This is important because prompts/specs may change later. The snapshot shows exactly what Gemini saw during that run.

### gemini_calls.jsonl

Logs every Gemini call.

Each line should contain:

- run ID
- timestamp
- agent name
- model name
- metadata
- full prompt text
- full response text
- status

This is the main traceability artifact.

### attempts/

Stores every generated attempt, even failed ones.

### generated_files/

Stores only files that passed both Logic Tester and UI Auditor.

### audit_logs.jsonl

Stores audit results per file and per attempt.

### applied_files.json

Records which generated files were applied to the target app repo.

### command_logs.jsonl

Records command outputs such as:

- `dotnet build`
- `git status`
- `git diff`

## Versioning Strategy

Do not create filenames like:

```text
erp_invoice_logic_v1.md
erp_invoice_logic_v2.md
final_final.md
```

Use stable filenames and Git commits.

Correct approach:

```text
stable filename + Git commit + run snapshot
```

When prompts or specs are refined:

```bash
git add prompts/ logic_specs/ ui_specs/
git commit -m "Refine Sprint 1 UI validation rules"
```

Then rerun the notebook. The run folder will capture:

- new Git commit hash
- snapshot of updated files
- new Gemini calls and responses

## Agent Output Safety

The notebook must not immediately overwrite the target app repo.

Recommended flow:

```text
generate -> audit -> save to generated_files -> human approve -> apply to app repo
```

## Feature Bundles

Some files should be treated as a bundle.

Example:

```text
Pages/Login.cshtml
Pages/Login.cshtml.cs
```

Even if one passes and the other fails, do not apply only half of the feature.

Future improvement: add `feature_group` and `depends_on` fields to the task JSON so the orchestrator can apply only complete feature groups.

Example:

```json
{
  "file_name": "Pages/Login.cshtml",
  "feature_group": "login_placeholder",
  "depends_on": ["Pages/Login.cshtml.cs"],
  "task_description": "Create the English-only Zen Green Login page."
}
```

Better grouped structure:

```json
{
  "feature_group": "login_placeholder",
  "files": [
    "Pages/Login.cshtml",
    "Pages/Login.cshtml.cs"
  ]
}
```

Rule:

```text
Only apply a feature group if all files in the group pass.
```

## Retry Strategy

Use:

```text
MAX_ATTEMPTS = 3
```

Retries should not be identical.

Use this pattern:

```text
Attempt 1: normal generation
Attempt 2: retry with audit feedback
Attempt 3: retry with audit feedback + stricter checklist + explicit final warning
```

More than 3 attempts usually means the task, specs, or prompt is unclear.

## Compact UI Checklist for Coder

When generating UI files, the Coder should be given this checklist:

```text
UI checklist:
- All UI text must be English only.
- Do not copy Thai labels from the PDF.
- Use Zen Green theme consistently.
- Use dark green #004d40 title/header strips with white text.
- Use light green/mint accents for borders, highlights, or selected states.
- Use rounded corners and clean enterprise spacing.
- Mark required fields with a red asterisk.
- Every action button must have an English tooltip/title.
- Use Font Awesome icons consistently.
- Numeric and currency fields must be right-aligned.
- Header field validation must appear as red text below the field.
- Line/grid validation must appear as floating tooltip-style messages.
- Forms must be responsive.
- Grids must support horizontal scrolling on small screens.
```

## Planned Sprints

### Sprint 1: App Shell and Foundation

Goal:

- app shell
- layout
- Zen Green theme
- landing page
- login placeholder
- menu structure

Known Sprint 1 task list:

```text
1. wwwroot/css/zen-green.css
2. wwwroot/css/site.css
3. Pages/Shared/_Layout.cshtml
4. Pages/Shared/_Layout.cshtml.css
5. Pages/Index.cshtml
6. Pages/Index.cshtml.cs
7. Pages/Login.cshtml
8. Pages/Login.cshtml.cs
```

In one run, 7 out of 8 tasks passed after two attempts. The failed file was:

```text
Pages/Login.cshtml
```

Recommended handling:

- apply only the passed foundation files
- hold back both `Pages/Login.cshtml` and `Pages/Login.cshtml.cs` as a feature bundle
- build and inspect the app shell
- commit the working foundation
- create Sprint 1.1 focused on the Login page

### Sprint 1.1: Login Placeholder Page

Goal:

- create English-only Login placeholder page
- database/environment selection
- username field
- password field
- Login button
- English tooltips
- Zen Green styling
- no Thai text

### Sprint 2: Authentication and Authorization

Goal:

- login flow
- user roles
- protected pages
- menu visibility
- possible user and role models

### Sprint 3: Product and Customer Management

Goal:

- product CRUD
- customer CRUD
- validation
- list views
- form views
- Tabulator grids
- LoV support where needed

### Sprint 4: Invoice Processing

Goal:

- invoice header
- invoice line items
- product lookup
- customer lookup
- calculations
- transactional save
- validation
- right-aligned numeric fields
- line-item grid validation

### Sprint 5: Reporting, Polish, and Final Audit

Goal:

- invoice print/view
- search/filter
- reporting
- UX cleanup
- final logic audit
- final UI audit

## Immediate Recommended Next Step

Since Sprint 1 had one failed file:

1. Keep the current run logs.
2. Apply only the passed foundation files.
3. Hold back both Login files.
4. Build and manually inspect the app shell.
5. Commit the working foundation in the app repo.
6. Create Sprint 1.1 for the Login placeholder page.
7. Update the Coder context to include the compact UI checklist.
8. Increase max attempts to 3.
